{
  "name": "calorie-tracker-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf lib",
    "deploy": "firebase deploy --only functions",
    "lint": "echo 'No lint configured'",
    "serve": "firebase emulators:start --only functions,firestore",
    "sync:once": "firebase functions:shell --project calorie-tracker-364e3"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
